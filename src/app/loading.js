import { LoadingExperience } from "@/components/ui";

export default function Loading() {
  return (
    <LoadingExperience
      title="Opening Aurify"
      message="Setting up your next study view."
    />
  );
}
