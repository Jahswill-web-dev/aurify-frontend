export const metadata = {
  title: "Studies | Aurify AI",
  description: "Create and continue focused Aurify AI study workspaces.",
};

export default function StudiesLayout({ children }) {
  return children;
}
