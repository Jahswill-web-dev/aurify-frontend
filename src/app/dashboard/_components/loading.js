import { LoadingExperience } from "@/components/ui";

function Loading() {
  return (
    <LoadingExperience
      variant="panel"
      title="Loading your dashboard"
      message="Pulling together your studies, summaries, and scores."
    />
  );
}

export default Loading;
